# 本轮增量：识别后暂停，确认后再分析

状态：对话工作环境中的待集成代码，未提交 GitHub、未部署、未修改用户电脑。

## 已写入本工作稿

上传入口只请求 recognize/clues；后端缺省也是仅识别，不再默认全跑。
新增持久确认记录，对 store/sample/clues/analysis_input/exclusions 绑定版本。
新增查看确认资料和确认开始接口。GET 不产生确认、不调用模型。
任何分析阶段需有效确认；识别和分析不能在一次请求中跨过确认关卡。
确认页展示识别商品和销售证据；默认保留商品，沿用既有排除操作。
分析期间资料编辑与任务使用同一单机锁；任务启动和后续阶段检查确认版本。
原始资料有变化、重新识别或排除商品后，旧确认不再有效。
旧分析保留但有待更新提示；没有把部分成功包装为已完成。
保留已有 RAG 默认策略，不增加自由需求框，不限定店铺类型。

## 明确未完成

销售值/商品名称逐项编辑、补商品编辑器；原项目完整预检、构建、浏览器验证；
实际模型识别、公司商品库和 Windows 端到端验证；GitHub 推送。

## 本轮测试

新增关卡及转换接线检查 46 项通过；包含这些项目的本地测试共 107 项通过；
另重跑原可靠性隔离测试 58 项通过。不得等同于完整项目测试。

新增文件和 v2 转换规则通过 apply_upgrade.py 在内存合并，再按原文件哈希预检应用。
没有取得完整原仓库，转换器全项目预检仍需在实际仓库运行；不保证未知版本可直接应用。
