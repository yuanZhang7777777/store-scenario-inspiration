# METIS 工作区与 UI 基准

核对时间：2026-09-22。正式 React / FastAPI 已实现两页流程、紧凑进度、完整分析、侧栏导航、默认场景全选和页尾导出。用户要求停止原型迭代，旧 HTML 原型已冻结；当前规格与验收以 [实现说明](2026-09-22-export-first-design.md) 为准。

## 文件在哪里

| 位置 | 职责 | 核对结果 |
|---|---|---|
| `E:/Project/store-scenario-inspiration` | 主仓库、`main` | `60fe47e`，不是正在运行的 UI 迭代版本 |
| 主仓库下 `.worktrees/store-analysis-ui-improvements` | UI 迭代工作区 | `feat/store-analysis-ui-improvements`，本轮起点 `3cb7a34`；原型和当前 React 应用在这里 |
| 主仓库下 `.worktrees/catalog-retrieval-foundation` | 检索与旧试跑工作区 | 独立分支，不作为这轮 UI 文件的搬运目标 |
| `E:/Project/store-assortment-copilot` | 产品资产、库存及历史试跑产物 | 非 Git 仓库，不是前端源码的另一份副本 |

读取到的 Vite 5173、FastAPI 8000 启动命令均指向 UI 工作区。主目录和 UI 工作区的 `var` 是两个普通目录，不是互相链接的同一个目录；不要将源码整理变成运行数据搬运。本次未修改 `.env` 或迁移业务数据。确认无运行任务后已重启后端到这份代码，显式保留 `var/demo` 数据目录；Vite 仍使用此工作区。

## 另一轮修改发生了什么

Git 记录中没有这轮源码目录被搬走的证据，确认发生的是提交、界面移植和回退：

| 提交 | 内容 | 当前状态 |
|---|---|---|
| `9f3ef6e` | 修正 Jev 复核置信度读取 | 已在 UI 工作区，保留 |
| `d78460f` | 交接文档与旧视觉稿 | 已记录；旧视觉稿不再作为实现依据 |
| `b80f22a` | 记录导出优先原型、设计与检查脚本 | 已在 UI 工作区，含原型增补 |
| `3cb7a34` | 正式应用改为蓝色配色 | 当前已提交基准；其上存在本轮已运行、尚未提交的正式 UI 改动 |
| `074aac7` | 移植同页分析、选择与导出布局，修改 10 个前端文件 | 20:24:55 被回退，当前未生效 |

已创建恢复分支 **`lxc/recovery-ui-port-20260922`** 指向 `074aac7`。没有重置当前工作区，也没有重新应用移植。需要参考时可用 `git show 074aac7:<文件>`，不必切换当前运行分支。

不直接整体恢复移植的原因：

- `StorePage.tsx` 用 `choice ?? (retrieval ? "preview" : "analysis")` 决定内容。阅读分析但尚未手动选择页签时，召回到达会切到商品清单；选择分析章节只设置 `chapter`，没有固定当前页签。
- `ExportCard.tsx` 列出“候选明细”工作表，但该提交没有修改后端导出器。当前实际工作簿为主 SKU、子 SKU、店铺概况、可选去重清单和口径说明；候选明细仍是待实施需求。

## 后续入口

| 要了解的内容 | 文件 |
|---|---|
| 冻结的早期交互原型 | [export-first-prototype](2026-09-22-export-first-prototype.html) |
| 当前设计决策、状态与验收 | [导出优先设计](2026-09-22-export-first-design.md) |
| 真实八步流程、接口与依赖 | [完整流程](2026-09-22-flow-and-branches.md) |
| 提供给设计方的业务背景与内容约束 | [流程简报](2026-09-22-flow-brief.md)，具体布局以现行两页方案为准 |
| 原有代码上下文 | [交接说明](2026-09-22-ui-handoff-brief.md)，顶部新决策优先 |
| 历史方向 | `2026-09-22-style-preview.html`、`2026-09-22-next-round-direction.md`，不作为当前 UI 规格 |

**原型与实际应用分开验收。** 原型计时、示例库存、模拟导出不移入业务逻辑；正式页面必须复用现有任务、产物、校验和导出接口。保留完整分析、排除/补充商品、参数修改、取消重试、库存未知提示及三级选择功能。

## 本次验证

- React：31 项前端测试、TypeScript / Vite 构建通过。
- Python：完整测试 500 通过、1 跳过；含新增就绪状态、过期阻断与店铺锁导出回归。
- HTTP：5173 页面和 API 代理通过，新元数据已生效；使用现有完成结果真实导出 XLSX，验证五张表与勾选货号一致。
- 后端稳定进程显式使用本工作区 `var/demo`，日志 `var/ui-api.stdout.log` / `var/ui-api.stderr.log`。后端改码后需重启；前端 Vite 保持热更新。
- 未操作浏览器，未进行视觉渲染验收或新付费模型调用。当前等待用户手测；未重置分支、移动数据或整体恢复 074aac7。
